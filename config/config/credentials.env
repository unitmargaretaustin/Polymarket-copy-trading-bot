# Keep secrets out of git. Load these into settings["credentials"].
# Add only what your login/execution flow requires.

PM_EMAIL=
PM_PASSWORD=

# If you use wallet-based login, you may prefer a persistent storage_state instead.
# Never hardcode private keys into automation.
